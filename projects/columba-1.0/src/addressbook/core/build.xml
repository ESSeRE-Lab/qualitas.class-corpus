<?xml version="1.0"?>

<project name="addressbook.core" default="compile">
	<target name="compile">
		<echo message="Compile Addressbook core"/>
		<javac source="1.4" target="1.4" srcdir="${src.addressbook.core}" destdir="${dest}" encoding="ISO-8859-1" debug="${debugbuild}">
			<classpath refid="compile.classpath"/>
			<exclude name="**/*.html"/>
			<exclude name="**/*.xml"/>
		</javac>
	</target>
</project>
